@model IEnumerable<FarmManager.Models.TerraPlantio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DTPlantio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DTColheita)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TPGrao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NRHectares)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DTPlantio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DTColheita)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TPGrao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NRHectares)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CDTerra }) |
            @Html.ActionLink("Details", "Details", new { id=item.CDTerra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CDTerra })
        </td>
    </tr>
}

</table>
