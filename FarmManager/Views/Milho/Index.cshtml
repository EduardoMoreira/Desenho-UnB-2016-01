@model IEnumerable<FarmManager.Models.Milho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NRQuantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DTAtualizacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NRQuantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DTAtualizacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CDMilho }) |
            @Html.ActionLink("Details", "Details", new { id=item.CDMilho }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CDMilho })
        </td>
    </tr>
}

</table>
