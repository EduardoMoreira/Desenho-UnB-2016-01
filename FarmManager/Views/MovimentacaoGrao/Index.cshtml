@model IEnumerable<FarmManager.Models.MovimentacaoGrao>

@{
    ViewBag.Title = "Index";
}

<h2>Registro de movimentação de grãos</h2>

<p>
    @Html.ActionLink("Criar novo registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DEMovimentacaoGrao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DEMovimentacaoGrao)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CDMovimentacaoGrao }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.CDMovimentacaoGrao }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.CDMovimentacaoGrao })
        </td>
    </tr>
}

</table>
